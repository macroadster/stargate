/**
 * Inscribe entry point — conversational WishBot nanobot (replaces the old form dialog).
 * Kept as InscribeModal so App.jsx and other callers keep working.
 */
export { default } from './WishChatModal';
